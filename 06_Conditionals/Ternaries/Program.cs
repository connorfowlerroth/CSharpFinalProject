// Here we're using Write instead of WriteLine
Console.Write("Enter your age: ");
string response = System. Console.ReadLine();
int age = int.Parse(response);

string output = age >= 18 ? "You can vote!" : "You're too young to vote.";
// Variable to hold returned value
// Boolean expression to be evaluated
// Value to be returned if 2 is true
// Value to be returned if 2 is false 

Console.WriteLine(output);