//  Declaring a variable to use in our loop's boolean expresion 
int number = 0;

// While the value in the number is less or equal to 100, run the following 
while (number <= 100)
{
    // Write the value of number to the console and then increment by 5
    Console.WriteLine(number);
    number = number + 5;
}
